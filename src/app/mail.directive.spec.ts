import { MailDirective } from './mail.directive';

describe('MailDirective', () => {
  it('should create an instance', () => {
    const directive = new MailDirective();
    expect(directive).toBeTruthy();
  });
});
